/* Reset styles */
* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

/* Global styles */
body {
  font-family: "Arial", sans-serif;
  background-color: #222;
  color: #fff;
  line-height: 1.5;
  display: flex;
  flex-direction: column;
  min-height: 100vh; /* Set minimum height to ensure the footer sticks to the bottom */
}

a {
  color: #fff;
  text-decoration: none;
}

/* Header */
header {
  background-color: #111;
  padding: 10px 20px;
}

.header-content {
  display: flex;
  align-items: center;
}

nav {
  flex-grow: 1;
  display: flex;
  justify-content: center;
  align-items: center;
  margin-right: 125px;
}

nav ul {
  list-style: none;
  display: flex;
  justify-content: center;
  align-items: center;
}

nav ul li {
  margin-right: 20px;
}

nav ul li:last-child {
  margin-right: 0;
}

nav ul li a {
  position: relative; /* Add relative positioning */
}

nav ul li a:after {
  content: "";
  position: absolute;
  bottom: -5px; /* Adjust the position of the underline */
  left: 0;
  width: 100%;
  height: 2px;
  background-color: #fff;
  transform: scaleX(0); /* Start with no underline */
  transition: transform 0.2s ease-in-out;
}

nav ul li a:hover:after {
  transform: scaleX(1); /* Apply the underline effect on hover */
}

/* Main */
main {
  flex: 1; /* Fill the remaining vertical space */
  padding: 50px 40px;
  margin-right: 120px;
  text-align: center;
}

section {
  margin-bottom: 40px;
}

h1, h2 {
  font-family: "Arial", sans-serif;
  font-weight: 700;
  color: #fff;
}

h1 {
  font-size: 18px;
  margin-bottom: 20px;
  line-height: 1;
}


h2 {
  font-size: 24px;
  margin-bottom: 10px;
}

p {
  margin-bottom: 20px;
  color: #ccc;
}

.button {
  display: inline-block;
  padding: 10px 20px;
  background-color: #007bff;
  color: #fff;
  border-radius: 4px;
  transition: background-color 0.3s ease;
}

.button:hover {
  background-color: #0056b3;
}

.name {
  color: #ffffff; /* Green color for the name */
}

.tagline {
  color: #007bff; /* Blue color for the tagline */
}

.branding {
  display: flex;
  align-items: center;
}

.branding h1 {
  font-family: "Raleway", sans-serif;
  font-weight: 700;
  font-size: 16px;
  color: #fff;
  margin: 0;
  line-height: 1;
}

/* Footer */
footer {
  background-color: #222;
  padding: 8px 20px;
  text-align: center;
  color: #ccc;
  display: flex;
  justify-content: left; /* Center the content horizontally */
  align-items: left; /* Center the content vertically */
  height: 40px; /* Set a fixed height to keep the footer centered */
}

footer p {
  font-size: 14px; /* Adjust the font size as desired */
}

/* Add the following CSS rules to your existing styles.css file */

section#about {
text-align: left;
}

.flex-container {
  display: flex;
}

.left-column {
  flex: 1;
  line-height: 1.1;
  padding-right: 20px;
  margin-left: 200px;
}

.right-column {
  flex: 1;
  line-height: 0.1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding-left: -10px;
}

.right-column img {
  max-width: 60%;
  height: auto;
}

section#research {
  text-align: left;
  margin-left: 150px;

}

hr.divide {
  align-content: left;
  background-color: whitesmoke;
  width: 80%;
  height: 1px;
  border-top: 1px solid black
}






